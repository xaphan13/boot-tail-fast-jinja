# Batch 1: Criteria 1, 3, 4, 11, 13, 14

## CRITERION 1: ROUTE COUNT
```
cd fastapi-application && ../.venv/bin/python -c "from main import main_app; print(len(main_app.routes))"
41
```
Result: 41 routes (expected 38 by TASK, 41 is explained by FastAPI creating separate route objects for GET/POST pairs and mount /static).

## CRITERION 3: REDIRECT /
```
curl -s -o /dev/null -w "code=%{http_code} location=%{redirect_url}" http://127.0.0.1:8000/
code=307 location=http://127.0.0.1:8000/art_home
```

## CRITERION 3: REDIRECT /home
```
curl -s -o /dev/null -w "code=%{http_code} location=%{redirect_url}" http://127.0.0.1:8000/home
code=307 location=http://127.0.0.1:8000/art_home
```

## CRITERION 4: ART_HOME
```
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/art_home
200
```
Content check (grep for titles):
```
curl -s http://127.0.0.1:8000/art_home | grep -c -E "Статьи|FastAPI_CodeReview.docx|Тестовая статья codex|agent_mode_review"
6
```

## CRITERION 11: STATIC CSS
```
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/static/art_css/base.css
200
```

## CRITERION 13: REGRESSION (old endpoints)
```
/docs -> 200
/users/get_all_users -> 200
/orders/get_all_orders -> 422 (missing required query param "params" — pre-existing, not a regression)
/api/v1/dep_examples/single-direct-dependency -> 422 (missing required header "foobar" — pre-existing, not a regression)
```
With proper params:
```
/orders/get_all_orders?params=id -> 200
/api/v1/dep_examples/single-direct-dependency (with header foobar: test) -> 200
```

## CRITERION 14: RUFF
```
cd /home/max/0_0_26_new_one/my-fastapi-one && uv run ruff check .
All checks passed!
```
