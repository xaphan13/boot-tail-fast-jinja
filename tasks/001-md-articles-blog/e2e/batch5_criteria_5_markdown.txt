# Batch 5: Criterion 5 (Markdown Render)

## CRITERION 5a: ARTICLE WITHOUT .md FILE CHECK
```
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/art/Max/1787932544
200
```
Note: File exists, returns 200 (not 404 as originally expected). The article renders.

### Existing article render check
```
curl -s http://127.0.0.1:8000/art/Max/1787932544 | grep -oP "(<h[12][^>]*>[^<]+|<pre><code|<table )"
<h1 class="art-title">FastAPI_CodeReview.docx
<table
<table
<table
<table
<table
<table
<table
<table
<table
```
Note: Uses `<h1 class="art-title">` for the article title, and has multiple `<table>` elements.

## CRITERION 5b: TEST ARTICLE RENDER
### Create test .md file
```
cat > fastapi-application/templates/content_art/qa_test_article.md << 'MDEOF'
# Заголовок один

## Подзаголовок

```python
print("hi")
```

| A | B |
|---|---|
| 1 | 2 |
MDEOF
```

### Add entry to articles.yaml (backup created at /tmp/articles.yaml.bak)
```
- author: QaTester
  lang: Python
  art_id: 9999000001
  title: QA Test Article
  file_name: qa_test_article.md
```
art_id count after adding: 6

### Render test article
```
curl -s -w "\nHTTP=%{http_code}" http://127.0.0.1:8000/art/QaTester/9999000001 | grep -oP "(<h[12][^>]*>[^<]*|<pre><code|<table |print\(\"hi\"\)|1 \| 2|HTTP=\d+)"
<h1 class="art-title">QA Test Article
<h2>Заголовок один
<h2>Подзаголовок
<pre><code
HTTP=200
```

### Heading verification
```
<h2>Заголовок один   — # Заголовок один rendered as <h2> (SEO h1->h2 replacement working)
<h2>Подзаголовок     — ## Подзаголовок rendered as <h2> (correct)
```

### Code block verification
```
<pre><code class="language-python">print(&quot;hi&quot;)
</code></pre>
```
Fenced code block rendered correctly with language class.

### Table verification
```
<table>
<th>
<th>
<td>
<td>
```
Markdown table rendered as HTML table.

## CLEANUP
```
rm templates/content_art/qa_test_article.md     — removed
cp /tmp/articles.yaml.bak md_articles/articles.yaml  — restored
diff /tmp/articles.yaml.bak md_articles/articles.yaml  — identical
grep -c "art_id:" md_articles/articles.yaml  — 5 (original count)
ls templates/content_art/qa_test_article.md  — No such file or directory (confirmed removed)
```
