# Batch 2: Criteria 2, 10, 12

## CRITERION 2: ALEMBIC CURRENT
```
cd fastapi-application && ../.venv/bin/alembic current 2>&1
INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
b59cbdf15878 (head)
```

## CRITERION 12: 404 ARTICLE
```
curl -s -w "\nHTTP_CODE=%{http_code}\nCONTENT_TYPE=%{content_type}" http://127.0.0.1:8000/art/x/999999
[full HTML page]
HTTP_CODE=404
CONTENT_TYPE=text/html; charset=utf-8
```
HTML contains: <h1>Страница не найдена (404)</h1>, <p>Такой страницы нет. Попробуйте перейти по другому адресу.</p>

## CRITERION 12: 404 NONEXISTENT PAGE
```
curl -s -o /dev/null -w "code=%{http_code} content_type=%{content_type}" http://127.0.0.1:8000/nonexistent-page
code=404 content_type=text/html; charset=utf-8
```

## CRITERION 10: CSRF WRONG TOKEN
```
curl -s -w "\nHTTP_CODE=%{http_code}\nCONTENT_TYPE=%{content_type}" -X POST -d "csrf_token=WRONG&email=test@test.local&password=x" http://127.0.0.1:8000/login
[full HTML page with title: 403 - Сайт о программировании]
HTTP_CODE=403
CONTENT_TYPE=text/html; charset=utf-8
```
HTML contains: <h1>Доступ запрещён (403)</h1>
