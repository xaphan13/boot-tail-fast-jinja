# Фаза 6: Страницы ошибок — DONE

- `errors/403.html`, `errors/404.html`, `errors/500.html`: единый шаблон —
  карточка max-w-lg, крупный градиентный код (.grad-text), заголовок h1,
  пояснение, кнопка «На главную» (btn-grad). extends layout сохранён.
- Checkpoint: 404 отдаёт HTML с data-theme, без navbar; код ошибки виден.
